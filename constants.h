#ifndef CONSTANTS_H
#define CONSTANTS_H

// Define o número máximo de bolas por tubo
const int MAX_BALLS_PER_TUBE = 4;

// Define o número máximo de bolas por cor
const int MAX_BALLS_PER_COLOR = 4;

// Define o número de cores usadas no jogo
const int MAX_COLORS = 4;

// Define o número máximo de tubos no jogo (cores + tubos extras)
const int MAX_TUBES = MAX_COLORS + 2;

#endif // CONSTANTS_H
